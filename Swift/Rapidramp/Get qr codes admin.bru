meta {
  name: Get qr codes admin
  type: http
  seq: 2
}

get {
  url: {{BaseURl}}/qr-codes/admin
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
