meta {
  name: Admin get qrcode transactions
  type: http
  seq: 7
}

get {
  url: {{BaseURl}}/qr-codes/transactions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
