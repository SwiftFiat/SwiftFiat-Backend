meta {
  name: Get stats
  type: http
  seq: 4
}

get {
  url: {{BaseURl}}/qr-codes/stats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
