meta {
  name: Get Admin stats
  type: http
  seq: 5
}

get {
  url: {{BaseURl}}/qr-codes/stats/admin
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
