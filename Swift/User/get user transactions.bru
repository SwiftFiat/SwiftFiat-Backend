meta {
  name: get user transactions
  type: http
  seq: 4
}

get {
  url: {{BaseURl}}/user/transactions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
