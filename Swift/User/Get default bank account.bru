meta {
  name: Get default bank account
  type: http
  seq: 8
}

get {
  url: {{BaseURl}}/user/bank-accounts/default
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
