meta {
  name: List configs
  type: http
  seq: 2
}

get {
  url: {{BaseURl}}/vault/admin/yield-configs
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
