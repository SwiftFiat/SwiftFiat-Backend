meta {
  name: Verify email
  type: http
  seq: 5
}

post {
  url: {{BaseURl}}/auth/verify-email
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "email": "ikwecheghu@gmail.com",
    "code": "2040"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
