meta {
  name: Logout all devices
  type: http
  seq: 3
}

post {
  url: {{BaseURl}}/auth/logout-all
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
