meta {
  name: Get total outflow
  type: http
  seq: 7
}

get {
  url: {{BaseURl}}/analytics/get-total-outflow-transactions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
