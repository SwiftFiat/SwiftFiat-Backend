meta {
  name: Admin get daily tx sumary
  type: http
  seq: 4
}

get {
  url: {{BaseURl}}/analytics/daily-transactions-summary
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
