meta {
  name: Get total rewards
  type: http
  seq: 8
}

get {
  url: {{BaseURl}}/analytics/get-total-reward-earned
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
