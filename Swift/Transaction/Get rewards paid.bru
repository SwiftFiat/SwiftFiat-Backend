meta {
  name: Get rewards paid
  type: http
  seq: 9
}

get {
  url: {{BaseURl}}/analytics/get-total-reward-paid
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
