meta {
  name: List cards
  type: http
  seq: 10
}

get {
  url: {{BaseURl}}/cards/list-cards
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
