meta {
  name: Register card holder
  type: http
  seq: 1
}

post {
  url: {{BaseURl}}/cards/register-card-holder
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "phone": "+2348118773038"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
