meta {
  name: Get card plans
  type: http
  seq: 21
}

get {
  url: {{BaseURl}}/cards/admin/get-card-plans
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
