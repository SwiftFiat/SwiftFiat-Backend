meta {
  name: Admin update card plan
  type: http
  seq: 22
}

post {
  url: {{BaseURl}}/cards/admin/update-card-plan/2
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "description": "Premium card with higher limits and no monthly fees"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
