meta {
  name: Admin list txs by user
  type: http
  seq: 19
}

get {
  url: {{BaseURl}}/cards/admin/list-card-transactions-by-user?user_id=1
  body: none
  auth: bearer
}

params:query {
  user_id: 1
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
