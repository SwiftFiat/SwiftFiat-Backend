meta {
  name: Card transaction list
  type: http
  seq: 17
}

get {
  url: {{BaseURl}}/analytics/list-card-transactions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
