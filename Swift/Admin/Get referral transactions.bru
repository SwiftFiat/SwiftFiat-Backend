meta {
  name: Get referral transactions
  type: http
  seq: 2
}

get {
  url: {{BaseURl}}/referral/admin/transactions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
