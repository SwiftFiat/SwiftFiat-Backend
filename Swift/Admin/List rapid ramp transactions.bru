meta {
  name: List rapid ramp transactions
  type: http
  seq: 14
}

get {
  url: {{BaseURl}}/analytics/list-rapid-ramp-transactions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
