meta {
  name: List admin notifications
  type: http
  seq: 7
}

get {
  url: {{BaseURl}}/analytics/list-admin-alerts
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
