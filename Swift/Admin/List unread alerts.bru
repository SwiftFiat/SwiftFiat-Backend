meta {
  name: List unread alerts
  type: http
  seq: 9
}

get {
  url: {{BaseURl}}/analytics/list-unread-alerts
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
