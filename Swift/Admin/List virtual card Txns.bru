meta {
  name: List virtual card Txns
  type: http
  seq: 12
}

get {
  url: {{BaseURl}}/analytics/list-card-transactions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
