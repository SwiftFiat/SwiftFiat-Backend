meta {
  name: Update system settings
  type: http
  seq: 11
}

put {
  url: {{BaseURl}}/analytics/update-system-settings
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "card_decline_fee": 3.0
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
