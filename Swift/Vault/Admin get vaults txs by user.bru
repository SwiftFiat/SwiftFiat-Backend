meta {
  name: Admin get vaults txs by user
  type: http
  seq: 9
}

get {
  url: {{BaseURl}}/vault/admin/transactions?user_id=1
  body: none
  auth: bearer
}

params:query {
  user_id: 1
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
