meta {
  name: Admin metrics
  type: http
  seq: 6
}

get {
  url: {{BaseURl}}/vault/admin/metrics
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
