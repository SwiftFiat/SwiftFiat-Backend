meta {
  name: Admin force deposit
  type: http
  seq: 8
}

post {
  url: {{BaseURl}}/vault/goals/admin-deposit
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "amount": "1000"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
