meta {
  name: chat
  type: http
  seq: 1
}

post {
  url: {{BaseURl}}/chat/message
  body: multipartForm
  auth: bearer
}

params:query {
  : 
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  text: how to pay bill?
}

settings {
  encodeUrl: true
  timeout: 0
}
