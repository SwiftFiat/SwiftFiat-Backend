meta {
  name: chat
  type: http
  seq: 1
}

post {
  url: {{BaseURl}}/chat/message?text=hello
  body: multipartForm
  auth: bearer
}

params:query {
  text: hello
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  text: How are you today?
}

settings {
  encodeUrl: true
  timeout: 0
}
