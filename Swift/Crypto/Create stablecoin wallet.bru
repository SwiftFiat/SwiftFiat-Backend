meta {
  name: Create stablecoin wallet
  type: http
  seq: 1
}

post {
  url: {{BaseURl}}/crypto/create-wallet
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "network": "eth",
    "currency": "USDT"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
