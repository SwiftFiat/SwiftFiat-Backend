meta {
  name: Create vip level
  type: http
  seq: 1
}

get {
  url: {{BaseURl}}/rate-manager/admin/vip-levels
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
