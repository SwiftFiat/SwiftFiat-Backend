meta {
  name: Get rules
  type: http
  seq: 5
}

get {
  url: {{BaseURl}}/rate-manager/admin/rules
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
