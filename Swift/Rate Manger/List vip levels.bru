meta {
  name: List vip levels
  type: http
  seq: 9
}

get {
  url: {{BaseURl}}/rate-manager/admin/vip-levels
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
