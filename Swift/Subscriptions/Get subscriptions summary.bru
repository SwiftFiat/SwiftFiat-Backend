meta {
  name: Get subscriptions summary
  type: http
  seq: 4
}

get {
  url: {{BaseURl}}/subscriptions/summary
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
