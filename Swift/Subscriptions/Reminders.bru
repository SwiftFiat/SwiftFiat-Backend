meta {
  name: Reminders
  type: http
  seq: 8
}

get {
  url: {{BaseURl}}/subscriptions/reminders
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
