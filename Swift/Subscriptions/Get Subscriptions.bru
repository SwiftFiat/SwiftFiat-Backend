meta {
  name: Get Subscriptions
  type: http
  seq: 1
}

get {
  url: {{BaseURl}}/subscriptions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
