meta {
  name: Admin list user referals
  type: http
  seq: 2
}

get {
  url: {{BaseURl}}/referral/admin/list?user_id=1
  body: none
  auth: bearer
}

params:query {
  user_id: 1
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
