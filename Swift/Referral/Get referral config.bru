meta {
  name: Get referral config
  type: http
  seq: 5
}

get {
  url: {{BaseURl}}/referral/admin/get-referral-config
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
