meta {
  name: Track referral
  type: http
  seq: 8
}

post {
  url: {{BaseURl}}/referral/track
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "referral_code": "okoro"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
