meta {
  name: Update withdrawal request
  type: http
  seq: 7
}

put {
  url: {{BaseURl}}/referral/admin/update-withdrawal
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "id": 3,
    "status": "approved"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
