meta {
  name: Get User badges
  type: http
  seq: 4
}

get {
  url: {{BaseURl}}/streaks/badges
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
