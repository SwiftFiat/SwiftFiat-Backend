meta {
  name: Get referral earnings
  type: http
  seq: 1
}

get {
  url: {{BaseURl}}/referral/earnings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
