meta {
  name: get referrals
  type: http
  seq: 8
}

get {
  url: {{BaseURl}}/referral/list
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
