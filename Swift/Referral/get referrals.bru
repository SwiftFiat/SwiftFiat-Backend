meta {
  name: get referrals
  type: http
  seq: 9
}

get {
  url: {{BaseURl}}/referral/list
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
