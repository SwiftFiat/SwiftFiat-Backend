meta {
  name: Acitvate reward config
  type: http
  seq: 6
}

put {
  url: {{BaseURl}}/rewards/admin/configure/3/activate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
