meta {
  name: Reward summary
  type: http
  seq: 2
}

get {
  url: {{BaseURl}}/rewards/summary
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
