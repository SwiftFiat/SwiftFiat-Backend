meta {
  name: Deactivate reward config
  type: http
  seq: 7
}

put {
  url: {{BaseURl}}/rewards/admin/configure/3/deactivate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
