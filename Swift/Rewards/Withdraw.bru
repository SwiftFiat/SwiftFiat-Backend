meta {
  name: Withdraw
  type: http
  seq: 12
}

post {
  url: {{BaseURl}}/rewards/withdraw
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "amount": 1000,
    "idempotency_key": "789w-9816puu"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
