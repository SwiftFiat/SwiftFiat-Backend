meta {
  name: Get reward statistics
  type: http
  seq: 8
}

get {
  url: {{BaseURl}}/rewards/admin/statistics
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
