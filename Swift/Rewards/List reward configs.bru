meta {
  name: List reward configs
  type: http
  seq: 5
}

get {
  url: {{BaseURl}}/rewards/admin/configurations
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
