meta {
  name: Get top reard users
  type: http
  seq: 9
}

get {
  url: {{BaseURl}}/rewards/admin/top-users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
