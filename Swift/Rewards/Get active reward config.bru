meta {
  name: Get active reward config
  type: http
  seq: 4
}

get {
  url: {{BaseURl}}/rewards/admin/config
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
