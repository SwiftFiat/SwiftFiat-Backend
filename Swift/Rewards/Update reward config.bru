meta {
  name: Update reward config
  type: http
  seq: 11
}

put {
  url: {{BaseURl}}/rewards/admin/configure/2
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "is_active": true
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
