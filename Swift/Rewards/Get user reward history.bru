meta {
  name: Get user reward history
  type: http
  seq: 10
}

get {
  url: {{BaseURl}}/rewards/history
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
