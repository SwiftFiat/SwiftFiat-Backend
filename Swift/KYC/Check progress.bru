meta {
  name: Check progress
  type: http
  seq: 1
}

get {
  url: {{BaseURl}}/kyc/verification-progress
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
